services:
  - type: web
    name: formboost-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start_all.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MINI_APP_URL
        sync: false
      - key: ADMIN_ID
        sync: false
